# =============================================================================
# lib/Target/CMakeLists.txt
# Aggregates the translation target libraries of the EmitRust project.
# =============================================================================

add_subdirectory(Rust)
