- [x] FR-220 (opened and LANDED 2026-09-09 from an owner question -- "is there
  a way to not emit dead code and remove the override"): **THE ANSWER WAS NOT
  TO STOP EMITTING IT -- AND MY OWN MEASUREMENT WAS CORPUS-BOUND IN A WAY THAT
  MADE THE OBVIOUS FIX UNLANDABLE.**
  MEASURED over the epoch-7 population with the blanket allow stripped and
  every crate built: only 33 of 294 crates warn, 56 warnings total, **and ZERO
  `function X is never used`.** By kind: 21 associated constants (C
  enumerators), 15 fields never read, 10 structs never constructed, 6 methods,
  4 variants. Every one is faithful translation of a declaration the C program
  made and did not use.
  **THE ZERO IS A PROPERTY OF THAT CORPUS, NOT OF THE EMITTER, and the plan
  built on it -- `dead_code = "deny"` -- was falsified by two oracles the plan
  itself named as gates.** epoch-7 IS `test/EndToEnd`: our own hand-written
  programs, which by construction call everything they define.
   - **c-testsuite regresses by 10.** An external-linkage C function uncalled
     in its own TU is not even a C warning; the deadness is an artifact of the
     emitter privatizing it for a `bin` crate. `00219` additionally produces
     `static A is never used`, a kind the tally also recorded as zero.
   - **It breaks FR-44's headline guarantee.** `RealWorld/Cpp` `polygon`'s
     `--incremental` crate stops building, because **recovery drops a rejected
     CALLER and orphans every function only that caller reached**. Recovery
     STRUCTURALLY MANUFACTURES dead functions, so "deny" and "a partially
     ported project must still compile" cannot both hold.
  Isolated precisely: remove only the `deny` line, keep every targeted
  attribute, and both ratchets pass. The attribution is oracle-safe; the hard
  error is not.
  **THE TRIPWIRE COMES FROM THE RATCHET INSTEAD.** `clippy_eval.py` filtered
  to `code.startswith("clippy::")`, so `dead_code` -- a rustc lint -- was
  never counted. A named `TRACKED_RUSTC_LINTS` allowlist now admits it. That
  works precisely because this evaluator measures the FROZEN EPOCH ONLY, never
  c-testsuite and never the `--recover` path, which is exactly where deadness
  is legitimate. Epoch-7 measures **0** `dead_code`, the pin stays **57**, and
  a newly dead function raises the tally and fails the gate -- with no hard
  error in the emitted crate, so recovery still builds.
  WHAT LANDED: the crate-root allow is GONE; `#[allow(dead_code)]` is written
  on each `struct_def`, on `enum_def` (**both** the transparent newtype and its
  sibling associated-constant `impl` -- one attribute does not reach the
  other), on data enums, on inherent `impl` blocks, and on the `__EmitrustFile`
  hosted-stdio VERBATIM BLOB, which is not an `emitrust.data_enum_def` and so
  never reaches `emitDataEnumDef`. `--preserve-c-names` keeps its three naming
  lints and gains no `dead_code`.
  **THE TRIPWIRE IS PROVEN BY POSITIVE CONTROL, not by assertion**: reinstating
  the blanket header makes both new tests fail (`NOBLANKET-NOT` and `DEAD`),
  and `clippy_eval --file-list` over one file with a dead static `fn` reports
  `1 dead_code`. Verified independently on a file holding FOUR dead things --
  an unused enumerator, an unconstructed struct, an unread field and a dead
  function -- where cargo reports **`generated 1 warning`**: only the function
  surfaces, and it is still EMITTED. Fidelity preserved; nothing is dropped.
  GOLDEN MOVEMENT, a complete LINE INVENTORY over 1,308 inputs rather than a
  hash count: 945 emitted-and-changed, 363 emitted by neither, and the entire
  diff is `945 x removed "#![allow(dead_code)]"` + `945 x removed blank line`
  + `1,432 x added "#[allow(dead_code)]"`. **Nothing else moved: the same
  items, attributed differently.** 22 goldens updated, each verified against
  its own oracle rather than re-blessed.
  **A DEFECT IN THE PREPARED WORK, found and fixed by the implementing agent:**
  six comments still asserted `dead_code = "deny"` after that was dropped, and
  one was worse than stale -- `rollbackTo` claimed *"recovery never leaves a
  dead `fn` behind. If it ever does, the emitted crate fails to build --
  loudly."* Measured false in ten seconds. It now states that recovery
  structurally manufactures dead functions, which is exactly why `deny` was
  unlandable. **The lesson is mine: I applied the no-deny variant without
  auditing comments that still described the deny design.**
  ALSO FOUND: the `depCrates` branch had to be reworked because
  `drop_back(")]\n")` cannot shorten an empty header -- and **no test pinned
  that branch.** One was added (`link-partition.c`, purely additive).
  Gate **1037/1037**; clippy **57 -> 57 (+0)** with `dead_code` at 0; binding
  both axes flat; c-testsuite 220/220 and Cpp17Suite 35/35; TRACTOR 41/252.
  **METHOD LESSON WORTH CARRYING: a zero measured over one corpus is a claim
  about that corpus.** epoch-7 is curated hand-written tests; c-testsuite is
  conformance code; the RealWorld projects are real. They disagree, and the
  plan rested on the one that could not see the problem.


