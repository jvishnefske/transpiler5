- [x] W2.9 By-value structured bindings (task-006). `emitStmt`'s DeclStmt
  case dispatches `DecompositionDecl` (IS-A VarDecl, so checked BEFORE the
  generic local path could mishandle it) to a new
  `emitDecompositionDecl`: the holding value imports through emitRValue
  (the trivial-copy CXXConstructExpr path unwraps a copy source; a
  factory-call initializer is a plain struct-returning call), materializes
  into an anonymous place, and each BindingDecl becomes an ORDINARY scalar
  local initialized from the zipped field (struct/pair sources) or
  element (std::array sources), registered in `symbols` — keyed by
  ValueDecl, so every later reference resolves exactly like a plain
  local's. Soundness: the holding object is a copy nothing else can
  alias, so per-binding locals differ from the standard's
  members-of-the-hidden-object semantics only in address identity, which
  the subset cannot observe; the EndToEnd test pins the copy semantics by
  writing a binding and printing the untouched source field. Two shapes
  whose desugar WOULD miscompile stay located rejections: the reference
  forms (`auto &[a, b]` aliases the source; a write through a per-binding
  copy would be lost) and a non-pair record whose binding is not the
  zipped field (the tuple-like protocol binds through get<i>; for
  std::pair the zip is exact by definition, and clang's own binding expr
  is checked for everything else). Authoring the tuple-protocol rejection
  pin flushed out a PRE-EXISTING crash: a class template partial
  specialization IS-A RecordDecl, and reaching `isByteRegionRecord` (via
  collectDeclTypeRecords' TU scan) or importRecord as a DEPENDENT type
  blew the stack inside clang's getTypeInfo (SIGILL) instead of
  rejecting; both sites now guard on `isDependentType()` (C records are
  never dependent, so the C path is untouched), and the std::tuple_size
  specialization needed to opt into the protocol already rejects as an
  unsupported top-level declaration — making the binding-level protocol
  guard unreachable belt-and-braces today, pinned as such.
  Gates: corpus 00601-00603 flipped UNSUPPORTED->PASS, Cpp17Suite ledger
  ratcheted to exactly 16 PASS / 10 UNSUPPORTED / 0 MISCOMPILE; new pins
  test/Import/Cpp/cpp-structured-bindings.cpp (split-file: holding-copy IR
  shape over struct/pair/array + the by-reference and tuple-protocol
  rejections) and test/EndToEnd/cpp-structured-bindings.cpp (byte-diff vs
  clang++: all three source shapes, factory-returned pair, and the
  write-does-not-leak-back copy pin); the 47-test C++ slice passes; the
  C-side byte-region/struct/union suite and the c-testsuite ledger are
  unchanged (216 passing, 0 miscompiles; the 4 sprintf-family entries
  failing belong to the separately-in-flight hosted-libc work recorded in
  W2.4's gates).
  (test/Import/Cpp/cpp-structured-bindings.cpp;
  test/EndToEnd/cpp-structured-bindings.cpp;
  test/Cpp17Suite/expected-pass.txt)

