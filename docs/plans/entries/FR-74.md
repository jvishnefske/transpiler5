- [x] FR-74 Member-array arguments decay to slice params (x7
  ArrayToPointerDecay in the 2026-08-14 external probe: the
  `compress(s->iv, s->leftover)` shape in tinycrypt's sha256/aes/cmac/
  ctr_prng — a struct-member ARRAY passed where a slice/cursor param is
  expected; reproduced synthetically: `bump(s.iv, n)` with
  `struct S { unsigned char iv[8]; ... } s;` rejects "unsupported
  pointer cast (ArrayToPointerDecay)" while the same call over a
  top-level array imports today). The fix: the argument-side region
  resolution accepts a MEMBER-ARRAY place — a dot/arrow projection
  chain ending at an array-typed field of an addressable struct place —
  as slice-argument backing, lowering exactly like a top-level array
  (SliceOfOp over the member place at cursor 0; same element-type
  agreement rules as FR-72). Scope: projection chains the place
  machinery already models (local struct dot access, nested members,
  and the owner-struct/global forms IF the spike shows the place
  resolves); everything unprovable keeps the verbatim rejection —
  especially decay in non-argument positions (pointer locals bound to
  member arrays stay rejected this wave). Aliasing: two member-array
  arguments from the SAME struct object into two mutable params reject
  (the existing same-base guard extends to the member chain); disjoint
  FIELDS of one struct are provably non-overlapping — admit only if
  the borrow image is legal Rust (two &mut borrows of one struct's
  different fields through SliceOfOp — spike must verify rustc accepts
  the emitted shape, else reject). Gates: Import pins
  test/Import/C/member-array-arg.c (byte and non-byte element member
  arrays to slice params, nested member, const form) + frontier pins
  (same-field twice mutably, member-pointer-local binding stays
  rejected, verbatim wordings); EndToEnd byte-diff
  test/EndToEnd/member-array-arg.c (the compress shape: struct with
  two member arrays passed to a mutating helper, argc-seeded, vs clang
  native); external re-probe expected to flip the sha256/ctr_prng
  compress-family sites (evidence, not gate); full lit 100%; C path
  only.
  **SPIKE VERDICT: GO (2026-08-14).** Hand-spliced IR for the compress
  shape (arrow base, dot base, disjoint-sibling-fields double-&mut)
  built from EXISTING ops only (member + slice_of), round-tripped the
  full pipeline, and byte-diffed the clang native; rustc accepts two
  simultaneous `&mut` of sibling fields, so the disjoint-fields shape
  is ADMITTED. Measured constraints that shipped: the interception
  lives in emitBorrowArgument's slice branch, NOT in
  emitPointerRValue's decay case — a general decay arm would leak
  member decay into pointer-local consumers that must keep their
  DIFFERENT verbatim wording ("pointer assigned a non-address value");
  global-struct-member bases stay rejected (globals ride the
  copy-in/copy-out or cell_slice conventions — a mut slice of the
  staged temp would silently lose mutations); the aliasing guard key
  became (VarDecl, field-path) with PREFIX-overlap semantics (same
  field twice rejects mut-or-shared for parity with the mutness-blind
  guard; whole-struct borrow = empty path catches &s + s.a; a naive
  struct-root key would wrongly reject the disjoint shape); union-arm
  chains and byte-region records excluded (arms overlap; region
  windows are not places); value args emit in a phase before borrows,
  making len-from-same-struct safe by construction. Admitted bases:
  local dot, nested dot chains (single-field inners flatten), arrow
  through mut_ref<struct> params. Full suite 585/585.

