- [x] FR-82 NO-GO (2026-08-15) Interior-member addresses of requirement globals (the
  measured residue of the IP_DATA front: 9 lwIP units take
  `&ip_data.<member>` via the ip.h macro family — ip6 61x, tcp_in
  73x, udp 32x — and FR-81's address-taken gate rightly rejects
  them; also the etharp `&ethbroadcast` shape FR-80's body-less-only
  scope excluded). SPIKE-FIRST WITH NO-GO FULLY EXPECTED as a
  possible outcome: this is place-projection THROUGH a trait
  boundary, and the sound images are genuinely constrained — a
  mutable global's interior cannot hand out `&'static` (no `&'static
  T` exists out of consumer Cell storage), and `&'static mut` is
  banned (unsafe). Candidate images the spike must differentiate or
  refute, each with its measured demand slice: (a) CONST interior:
  for FR-80-style const requirement globals, `&g.member` extends the
  existing &'static getter by projection (`&E::g().member` on a
  &'static base is itself &'static) — covers the etharp shape and
  any const interior use; (b) READ-ONLY interior of a MUTABLE
  global where the address flows only into CONST-pointer parameter
  positions within one statement — the borrow can be a TEMPORARY
  (`&E::g().member` on the Copy temporary), exact iff the callee
  only reads during the call and identity is never compared —
  measure whether lwIP's uses satisfy that (macro-expanded calls
  like ip4_addr_cmp(&ip_data.x, p) do; storing the pointer does
  not); (c) WRITE through the interior address
  (`ip_addr_set(&ip_data.x, v)` shapes): only expressible as
  read-modify-write IF the callee is importable and inlined-through
  — likely stays rejected; measure the split. The spike must
  produce per-unit, per-shape counts for (a)/(b)/(c) across the 9
  units BEFORE any implementation choice, and NO-GO records that
  table. Gates (if any GO subset): pins per admitted shape with
  frontier arms for the rest; EndToEnd byte-diff exercising the
  admitted image against a clang native; lwIP re-probe with
  measured un-error counts; full lit 100%; C path only.
  **SPIKE VERDICT: NO-GO (2026-08-15), with the measured table.**
  A clang-AST place-projection automaton classified all 258
  address-involving ip_data occurrences across the 9 units:
  FOLD-READ 177 (the `&` immediately cancelled by `->`/`*` — a
  direct read, no pointer survives; 158 cross the u_addr union
  arm), FOLD-WRITE 34 (all union), PTRCMP-NULL 13 (statically-true
  IP_IS_V6 macro compares), CALL-CONST 29 (the only true class-(b)
  shapes; 15 union-blocked), CALL-INDIRECT 1, ESCAPE-STORE 4.
  Class (a) = 0 (ip_data is mutable); class (c) CALL-MUT = 0.
  THREE decisive facts: (1) 220/258 occurrences cross the u_addr
  union arm, which the FR-78 opaque model rejects BEFORE any
  requirement machinery — admitting the maximal FR-82 subset flips
  ZERO of 9 units; (2) the (b) temporary-borrow image was byte-diff
  MEASURED to diverge when the callee writes the global through its
  own requirement access (native hazard=99 vs Rust 41), and with
  body-less solo-TU callees "callee never writes g" is unprovable
  in-importer — admission would need explicit contract machinery,
  never a silent assumption; (3) etharp's real ethbroadcast is a
  packed byte-region record with no StructDefOp — fails FR-80
  admission independent of the body-less-only scope. POSITIVE
  residue for any future wave: the (b) image needs ZERO new ops
  (staged variable + member + addr_of renders `&v1.member` through
  the existing pipeline — hand IR proven), and the CHEAP FOLDS
  (deref-cancelled `&` = 211 sites semantically identical to what
  FR-81 already lowers, gated only by the syntactic
  addressBoundGlobal pre-scan; + 13 constant-true null compares)
  pay off jointly with ONE prerequisite: opening the FR-78
  opaque-union model for the dual-stack ip_addr_t (or an IPv4-only
  lwIP config). THE REDIRECT: the actionable next front is the
  union-arm model, after which the folds alone flip ip6_frag
  immediately. All current rejections stay located and pinned.

