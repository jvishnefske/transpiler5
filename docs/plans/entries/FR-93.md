- [x] FR-93 Pointer locals bound to member arrays (aes.c's last
  blocker: `uint8_t *Iv = ctx->Iv;` at aes.c:504 — "pointer
  assigned a non-address value" — the non-argument member-decay
  form every wave since FR-74 deliberately kept rejected; also
  measured in the tinycrypt residuals as escaping-store shapes).
  The candidate: a pointer LOCAL initialized from (or assigned) a
  member-array decay extends the existing (backing, cursor) local
  convention with a MEMBER-PLACE backing — the local's backing is
  the member place (or the byte-region window per FR-91 for
  byte-region roots), its cursor starts at the member's offset,
  and every existing local-pointer operation (deref, subscript,
  arithmetic, argument passing) composes over that backing exactly
  as over a top-level array backing. The aliasing surface must be
  measured: the local's borrow lifetime vs subsequent direct uses
  of the root (aes's Iv local coexists with ctx->RoundKey windows
  in the same loop — cursor-model locals materialize borrows
  per-use, not held, so this may compose for free; the spike must
  verify with the real cbc loop shape). Frontier: reassignment
  across DIFFERENT roots, escaping stores, address-of the local.
  Gates: Import pins (init and assign forms, deref/subscript/
  arithmetic/argument uses over the member backing, the cbc loop
  shape) + frontier arms; EndToEnd byte-diff (the cbc walk vs
  clang native, argc-seeded); external re-probe (aes.c
  cbc_encrypt/decrypt measured — 24/24 is the target); full lit
  100%; C path only.
  **SPIKE VERDICT: GO (2026-08-15) — two-phase, and the deepest
  wave of the arc.** The spike found the entry UNDER-scoped: the
  cbc shape has a SECOND blocker layer beyond the member decay
  (multi-base slice arguments — "pointer bound to multiple
  objects" — plus the aliasing-args check), and the "composes
  free" borrow hypothesis FAILED measured borrowck (E0502): the
  same-base (mut,const) pair needs a whole-call dispatch with a
  split_at_mut arm — byte-diff-proven sound (const window bounded
  below the mut cursor; out-of-window reads panic, the
  house-approved loud refinement). Landed WITHOUT the new dialect
  op the spike thought necessary: the split primitive is the
  verbatim helper __emitrust_split_mut_u8 through the EXISTING
  multi-result call_opaque rendering (the pool_unpack precedent).
  Window-backed locals are plain region bases with absolute
  cursors (no member-keyed multi-base entries — typed member
  bases stay rejected, keeping the cross-root frontier located);
  the classifier intercepts in storePointerAssign to keep every
  FR-91 argument-position pin byte-identical. Dispatch scope:
  exactly ONE multi-base pointer argument per call, direct void
  callees. Escaping stores keep the static-binding guard; the
  24/24 target was corrected — record `missing` is INHERENT to
  the byte-region model (a fully-ported green file still reports
  its record missing), so 23/24 is the true ceiling. tinycrypt
  residuals confirmed out-of-scope (member-FIELD escapes).
  Full suite 651/651.

