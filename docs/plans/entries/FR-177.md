- [ ] FR-177 (opened 2026-08-30 by a PMF-criterion re-ranking of the TRACTOR
  corpus, and it CORRECTS FR-138's top-ranked lever): **THE SPHINCS+ BUNDLE
  IS A FIVE-FIX WALL, NOT A ONE-FIX LEVER, AND THE FIRST TWO FIXES ARE WORTH
  ZERO CASES.**
  FR-138 ranked the 211 EMIT_FAILs by FIRST-failure diagnostic and concluded
  "aliasing mutable pointer arguments 80 (the ENTIRE SPHINCS+ `_lib` bundle,
  all from ONE shared `app/src/utils.c:79` -- **a single fix moves 80
  cases**)". MEASURED, and that is FALSE. It is the FOURTH time this ledger
  has read a first-failure count as a yield (FR-61f twice at 7x and 4.4x,
  FR-165 at 5.6x, now this).
  METHOD: for all 128 SPHINCS+ cases, emit with `--recover --incremental`
  from the harness's own per-case compile database and collect the SET of
  distinct blocking diagnostics per case, rather than the first one. Strict
  mode -- which is the only mode the rubric scores, because there is no
  partial credit -- fails on ANY rejection, so a case clears only when its
  WHOLE set is cleared.
  MEASURED, 128 of 128 cases:
    cases where aliasing is the ONLY blocker ........... 0
    blocker-set size -> cases ...... {3: 32, 5: 48, 12: 36, 13: 12}
  GREEDY MARGINAL-YIELD CURVE (cumulative fixes -> cases cleared):
    +1  aliasing mutable pointer arguments ................  0
    +2  string function argument must designate a char array  0
    +3  non-constant array size ........................... 32
    +4  address of a scalar object cannot be passed ....... 32
    +5  pointer cast (ArrayToPointerDecay) ................ 80
    +6..+12  (every remaining class) .................... 80
  So the real shape is: THREE fixes buy the first 32 cases and FIVE buy 80;
  one buys nothing. The remaining 48 cases need 12-13 classes and are not
  worth costing until the first five land.
  WHY THE ERROR MATTERS BEYOND THE ARITHMETIC: a first-failure histogram
  ranks by what the clause chain happens to report first, which is an
  ordering artifact of the importer, not a property of the corpus. Under a
  no-partial-credit rubric the only honest instrument is the per-case
  blocker SET and the marginal-yield curve over it. Rank nothing on the
  TRACTOR corpus by first-failure again.
  BASELINE RE-MEASURED AT HEAD (f64e7d4), same harness, same corpus, all 252
  cases: **PASS 25 / VACUOUS_PASS 1 / SYMBOL_MISSING 15 / EMIT_FAIL 211** --
  identical to the 2026-08-28 figure except SYMBOL_MISSING 13 -> 15. So the
  ~26 commits between those two points moved the TRACTOR score by ZERO,
  which is itself the finding a PMF criterion exists to surface: the
  internal backlog rank and the external scoreboard had fully decoupled.
  THE CORPUS SPLITS IN TWO, and the halves want different work:
    SPHINCS+     128 cases (51%) -- ALL EMIT_FAIL, behind the 3/5-fix wall
    non-SPHINCS  124 cases -- 25 PASS, 1 VACUOUS, 15 SYMBOL_MISSING, 83
                 EMIT_FAIL whose first-failure tail is long and flat
                 (max 9: variadic call; then 8, 6, 5, 4, 4, 4, 4, 3 ...)
  The 15 SYMBOL_MISSING are the highest-CONFIDENCE population on the board:
  they already emit AND build, and only FR-139's all-scalar `--c-abi-exports`
  restriction stops them exporting a dlsym-able symbol. They are also a
  direct rubric conflict -- FR-139 refused pointer exports because they need
  `unsafe`, i.e. "spending rubric criterion (b) to buy (a)" -- and FR-139
  measured a real miscompile there (`crc16` native 27235 vs cdylib 0), so
  that population needs its own spike before any of it is claimed.
  **NOT SPIKED beyond this ranking.** The next step is to cost the three
  cheapest classes (aliasing, string-function-argument, non-constant array
  size) INDIVIDUALLY, because 32 cases is the first non-zero step and
  nothing below three fixes pays at all.

