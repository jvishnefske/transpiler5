- [x] FR-92 The 2D-array-pointer cast shape (aes.c's remaining
  core: `typedef uint8_t state_t[4][4]; Cipher((state_t*)buffer,
  ctx->RoundKey)` and Cipher's body indexing `(*state)[i][j]` —
  a u8 buffer reinterpreted as a fixed 4x4 byte matrix; the
  standalone probe rejects "unsupported pointer expression:
  CStyleCastExpr" and Cipher's own body fails on the state_t*
  param). Census-first: what does the multidim byte matrix need —
  (a) a state_t* param mapping (mut_ref over a 2D array or a
  flattened 16-byte slice with row-major index arithmetic — the
  FR-83 byte-view precedent computes offsets at import; i*4+j is
  a compile-time-affine index), (b) the cast admission (a u8
  region view reinterpreted at the SAME element type u8 — no
  transmute, just shape — the FR-71 pointee-wildcard family), and
  (c) the body's (*state)[i][j] lowering over the chosen
  representation. NO-GO recordable per sub-shape with the census.
  Gates: Import pins + frontier arms (measured wordings); EndToEnd
  byte-diff (the Cipher-shaped matrix walk vs clang native,
  argc-seeded); external re-probe (aes.c Cipher/tu0_cipher
  measured — the unit's remaining stubs ride on it); full lit
  100%; C path only.
  **SPIKE VERDICT: GO (2026-08-15).** The census found the BODY was
  never the problem — nested ArrayType composes, subscript-of-
  subscript renders `s[i][j]`, and the 9-function state_t* family
  already ports as `&mut [[u8; 4]; 4]` (the mout2 crate pins it —
  so the representation was FIXED by byte-identity, no flatten).
  Exactly two blockers: the param CLASSIFICATION (collectSliceParams'
  call-arg rule forced Slice, whose element check rejects nested
  arrays — a positional forwarded-array-pointer exception keeps it
  ScalarRef; the pointee-keyed form would have moved the pinned
  MULTIDIM wording, so position won) and the CAST (landed as a
  call-argument-position admission: the existing generic reslice
  yields &mut [u8], then an on-demand module-level verbatim helper
  `__emitrust_chunk_RxC` — as_chunks_mut + try_into, the cstr_out
  precedent — reshapes it; statically-short sources get a new
  located rejection arm; element-changing casts, VLA dims,
  row-addresses, state_t* locals keep measured wordings). Rust's
  implicit reborrow makes bare double-forwarding legal. Byte-diff
  covers three cast flavors incl. a walking cursor at nonzero
  mid-loop offset. External ceiling recorded honestly: 22-23/24
  (cbc_encrypt carries an independent Iv-local blocker).
  Full suite 645/645. Measured post-land: aes.c 22/24.

