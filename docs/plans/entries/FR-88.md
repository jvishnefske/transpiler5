- [x] FR-88 Nullable byte-slice parameters as Option-wrapped slices
  (the FR-87 spike's named frontier: tinycrypt's
  tc_ctr_prng_init/reseed/generate stub on "string function argument
  must designate a char array" because a pointer param that MAY BE
  NULL — `if (personalization) memcpy(..., personalization, ...)` —
  cannot be a plain slice; the fn-ptr precedent already models C
  NULL as Option::None). The candidate convention: a byte-pointer
  param whose uses are null-COMPARISONS plus region/slice uses
  under the null guard classifies as `Option<&[u8]>`-shaped
  (`ref<slice>` wrapped the way fn-ptr params wrap today — check
  what the dialect has: fn_ptr uses Option natively; is there an
  option-of-ref type or does this need a new type/attr?); call
  sites pass `Some(®ion)` or `None` for the NULL constant; the
  null test lowers to is_some/is_none; the guarded region use
  unwraps within the guard (the pattern the C idiom guarantees —
  a use NOT dominated by the guard is the frontier). Spike must
  establish: the dialect surface (new type vs existing Option
  machinery), the dominance/guard analysis needed to prove every
  region use sits under the null test (conservative: only the
  `if (p) { ... }` / `if (p == NULL) return` shapes — measure the
  tinycrypt and broader corpus demand), call-site coercions, and
  the FR-75 requirement-signature interplay (a declaration-only
  nullable param in a trait item). NO-GO recordable. Gates: Import
  pins (the guarded-memcpy shape, both guard polarities, None and
  Some call sites) + frontier (unguarded use, arithmetic on the
  nullable param — measured wordings); EndToEnd byte-diff (NULL and
  non-NULL paths both exercised, argc-seeded, vs clang native);
  external re-probe (ctr_prng init/reseed/generate measured); full
  lit 100%; C path only.
  **SPIKE VERDICT: GO (2026-08-15).** Both pivots resolved with ZERO
  new dialect surface: fn_ptr's baked-in Option cannot be reused but
  doesn't need to be — the convention is opaque<"Option<&[u8]>">
  params + the ALREADY-PINNED method_call is_some/unwrap machinery
  (C99-43's Option<i64> precedent) + call_opaque "Some"/opaque None
  constants; trait items render Option<&[u8]> through generic
  emitType. Measured discoveries that shaped the wave: the real
  ctr_prng blocker was the ui8 LOCAL-ARRAY region (emitCharRegion-
  Slice's i8-only array branch — admitted alongside); the cast-null
  family's AST wraps NullToPointer in an ImplicitCastExpr BITCAST
  (defeating isNullPointerConstantExpr — peel loop added); clippy's
  unnecessary_unwrap boundary was MEASURED (the let-bound guard bool
  from MethodCallOp's always-let rendering is load-bearing — never
  inline single-use method_call results into if conditions); and THE
  FOLD-FLIP TRAP: today's constant-folding of `if (p)` on slice
  params is sound only because NULL call sites reject — Nullable
  classification bypasses the fold (a miss fails LOUD as a None-path
  unwrap panic, and the EndToEnd gate executes the None path).
  Scope: const-pointee only (Option<&mut [u8]> is not Copy — located
  rejection); positive-polarity and early-exit guard shapes;
  byte-family SOURCE positions (subscript reads and call pass-through
  decline to today's behavior — sound, the null sites still reject);
  declaration-only callees do not grow Option signatures this wave.
  Both argc-seeded paths byte-identical. External: the ctr_prng
  blocker is REMOVED and the next distinct frontier exposed and
  measured (member-address arguments — the FR-89 candidate).
  Full suite 633/633.

