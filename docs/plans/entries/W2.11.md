- [x] W2.11 `std::optional<T>` -> `Option<T>` (task-008; flips Cpp17Suite
  00904; VALIDATED 2026-08-10: full lit 554/554, ledger exactly 20 PASS /
  6 UNSUPPORTED / 0 MISCOMPILE). The C++17 vocabulary type whose Rust image is exact: recognition
  maps `std::optional<T>` to `!emitrust.opaque<"Option<S>">` for element
  types already in the STL element set (`rustSpellingForElementType`, the
  same gate the `Vec<T>` arm uses), construction lowers to `Some(v)` /
  `None`, and the two corpus methods lower to their exact Option
  counterparts: `has_value()` -> `is_some()` (i1), `value_or(d)` ->
  `unwrap_or(d)` (receiver `Option<i32>` is Copy in Rust, so the
  by-value-consuming `unwrap_or` on an lvalue place compiles without a
  clone). Recognition sites, all existing machinery: a `mapStdLibraryType`
  arm (ImportCTypes.cpp, next to vector/array/pair), `isStlOpaqueType`
  accepting the `Option<` spelling, `emitStlConstruct` arms for the
  default ctor and the `nullopt_t` converting ctor (-> `emitrust.literal
  "None"`, the FR-52 Option-cursor precedent) and the element converting
  ctor (-> `emitrust.call_opaque "Some"(v)`), the value-position
  `CXXConstructExpr` route for construct-at-return, a `CallExpr`
  initializer for STL locals (C++17 guaranteed elision hands
  `optional<int> a = find_even(8);` to the importer as a bare CallExpr —
  TODAY a located rejection for every STL local), and an
  `emitStlMemberCall` optional arm. Copy/move construction
  (`optional<int> b = a;`), `operator=` reassignment, `value()` (throws
  where Rust panics), `operator*`/`operator bool`, and nested/non-scalar
  elements stay LOCATED rejections this wave.
  **SPIKE VERDICT: GO (2026-08-10).** Hand-drove the emission end-to-end:
  (a) a hand-written crate in the emitter's exact shape (`Some(v)`/`None`
  returns, `is_some() as i32`, `unwrap_or(-1)`) is byte-IDENTICAL to the
  clang++ native for 00904 ("1 8 0 -1"); (b) dialect round-trip through
  emitrust-translate confirms `call_opaque "Some"` renders `Some(v0)`
  (and the tail return folds via FR-61a), `literal "None"` renders bare
  `None` (no turbofish needed — the let/return position types it),
  `method_call` renders `.is_some()`/`.unwrap_or(v0)`, and an opaque
  return type renders `fn find_even(v0: i32) -> Option<i32>`. No new
  dialect op needed.
  Gates: corpus 00904 UNSUPPORTED->PASS, Cpp17Suite ledger ratchets to
  exactly 20 PASS / 6 UNSUPPORTED / 0 MISCOMPILE; new pins
  test/Import/Cpp/stl-optional.cpp (Option local from a call initializer,
  Some/None construct-at-return, is_some/unwrap_or lowering, the opaque
  return signature) and stl-invalid.cpp frontier pins (copy-construct,
  `value()`, `operator*`); new test/EndToEnd/stl-optional.cpp byte-diffs
  the built crate against clang++ with argc-derived seeds covering
  engaged and empty on both call sites; full lit 100%; C path untouched.

