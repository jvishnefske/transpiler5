- [x] FR-218 (opened and LANDED 2026-09-09; FR-167 PHASE 2, the last of its
  two phases): **THE UNION-DECLARATION FRONTIER IS CLOSED TO TWO DELIBERATE
  SOUNDNESS EXCLUSIONS -- AND THE SPIKE THAT RANKED THIS WORK FELL INTO THE
  VERY TRAP IT CATALOGUED.**
  Phase 1 put a trial/rollback in `collectRecordFields`; phase 2 does the same
  one level down, inside `collectUnionSlot`, falling back to the FR-78 sizeof
  blob when the union is C, non-empty, and has no bit-field or
  incomplete-array arm. `collectUnionSlot` became a wrapper over the old body;
  the new `appendOpaqueUnionBlob` also replaces the inline blob construction
  in FR-78's `allArmsAggregate` branch, so there is now ONE blob builder.
  **BYTE IDENTITY WAS CLAIMED BY CONSTRUCTION AND WAS ACTUALLY VERIFIED, with
  the split-file sweep FR-167's own method correction demands.** Whole-file,
  hashing stdout+stderr+rc for `--emit=rust` and `--emit=mlir` over 1,109
  sources: 2 changed, one the new test and one a rejection moving LATER
  (`test-entry-section.c` stays rc=1, its first fatal error moving from
  `union with a pointer arm` to `non-constant global initializer` as its two
  fn-ptr arms blob). Split-file-aware, 187 tests expanded to 1,187 sub-units x
  2 modes: 18 moved, 5 of them new; **all 13 pre-existing movers had
  `base_rc=1`, and ZERO sub-units with `base_rc=0` changed a byte.** The claim
  holds, and it holds because it was measured.
  **THE FR-215 HYPOTHESIS: TWO SITES CONFIRMED, ONE REFUTED, AND THE
  REFUTATION IS THE FINDING.**
    `NLAPolicy.policy_set`            133 predicted -> 137 resolved, 0 left ✔
    `NLAPolicySetUnionElement.string`   7 predicted ->   7 resolved, 0 left ✔
    `sd_json_variant.parent`           48 predicted ->   0 resolved, 33 left ✘
  `sd_json_variant` has **TWO** anonymous unions. The one FR-215 named
  (`n_ref`/`parent`, sd-json.c:70) IS unlocked and is in the resolved set. But
  forty lines later a second anonymous union carries
  `DECLARE_FLEX_ARRAY(char, string)` -- an INCOMPLETE-ARRAY arm, which the
  eligibility screen deliberately excludes because `sizeof` does not cover a
  flexible tail and a blob sized by it would **silently drop storage the C
  program reaches**. So the record still rejects, now at sd-json.c:110.
  **FR-215 attributed 48 items to `parent` because `parent` was merely the
  FIRST failure -- a further instance of the first-failure trap that FR-215
  ITSELF CATALOGUED as the sixth occurrence.** Those 48 need a FAM-arm union
  representation, not phase 2.
  **AND THE 193 WERE UNLOCKED AS CASCADES, NOT AS POINTER-WORDED ITEMS**:
  `pointer type outside a parameter position` moves only 723 -> 720 in raw
  containment (7 direct resolved, 4 surfaced). FR-215 was right that the
  amplifying bucket was a union item; it was wrong about which union.
  LEDGER, same-method census over all 1,604 unique `.c` units of the
  configured tree (FR-215's 501-object pin is no longer on disk, so absolute
  counts are NOT comparable to the spike's): deduped blocked **53,916 ->
  53,876, NET -40** (304 resolved, 264 surfaced), **28 symbols blocked ->
  clean and ZERO clean -> blocked**, 749 of 1,604 units improved.
  **THE UNION-DECLARATION FRONTIER IS ESSENTIALLY CLOSED**: `union with a
  pointer arm` 15 -> 0, `union with an unnamed arm` 5 -> 0, `union arm cannot
  alias the storage slot` 4 -> 0, `union arms of differing sizes` 1 -> 0,
  `union type` 12 -> **2**. Both residuals are the two deliberate soundness
  exclusions: the FAM-arm union above, and `compress.c:169` (`Compressor`),
  which with all five `HAVE_XZ/LZ4/ZSTD/ZLIB/BZIP2` at 0 in this config has
  **no arms at all** -- the empty-union case.
  FR-167's 2026-08-30 prediction that phase 2 converts ~131 items into
  `global initializer for this type` **re-measured at 129** ten days and
  several waves later, plus 21 `opaque union arm initializer`. Still nets
  better than phase 1, though at -40 rather than the spike's projected -127.
  SIX PINNED FRONTIER REJECTIONS FLIPPED, all `not emitrust-import-c` legs in
  `test/Import`, **none loosened** -- each replaced by an exact
  `["opaque"] [!emitrust.array<Nxui8>]` shape check carrying the blob's
  `sizeof`, or by a strictly-later located rejection. No pinned EndToEnd
  source was edited, so epoch-7 stays open.
  ALIGNMENT COST, INHERITED NOT INTRODUCED: the blob is `[u8; N]` at Rust
  align 1 where the C union's align is its widest arm's. `abiFaithfulnessBlocker`
  already refuses unions wholesale, so no false ABI claim is emitted -- but a
  `sizeof`/`offsetof` fold on a STRUCT CONTAINING such a union comes from
  clang's layout while the emitted Rust struct's does not. Phase 2 WIDENS the
  population of that pre-existing FR-78 property; it does not create a new
  class, and fixing it would move FR-78 goldens and the byte-view machinery.
  Gate **1033/1033**; clippy 57 -> 57 (+0) at the epoch-7 pin; binding both
  axes flat (free_temps 2307, p95 5); both corpus ratchets unmoved; TRACTOR
  **41/252** with the external probe's `results.tsv` byte-identical on both
  sides (that corpus has no union-family items). The 12 most-improved systemd
  crates `cargo build --release --offline` clean on BOTH sides -- the FR-173
  E0609 backstop class.

