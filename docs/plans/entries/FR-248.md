- [ ] FR-248 (opened 2026-09-12, found by ratchets.yml's FIRST dispatch --
  which is the visibility that workflow was built for): **THE CLIPPY DEBT
  COUNT IS ENVIRONMENT-SENSITIVE BY EXACTLY ONE `needless_late_init`, AND
  NOBODY KNOWS WHICH SIDE IS WRONG.**
  MEASURED, same commit (703cf85), same flake.lock, same epoch pin, same
  294-crate corpus, 0 skipped on both sides: the GitHub runner counts
  **58** total warnings where a local run counts **57**, and the entire
  delta is `clippy::needless_late_init` 8 vs 7 -- every other lint count
  is identical (full-report diff over the --json outputs, both retained
  as FR-246 evidence). Local runs are stable at 57 across days (FR-241
  recorded 57 (+0) on 2026-09-10). The ratchet therefore FIRES on CI and
  passes locally, with the pinned baseline itself measured locally.
  HYPOTHESES, none yet tested, with permission to refute all of them:
  (a) a caching asymmetry in how clippy_eval.py invokes cargo clippy that
  suppresses one repeat warning locally (which would mean 58 is the truth
  and the local instrument UNDERCOUNTS); (b) genuine clippy
  nondeterminism in needless_late_init on one crate; (c) an
  environment-dependent difference in one emitted crate, which would
  violate the byte-identity doctrine and be far more serious than the
  lint count. The first diagnostic step is cheap and decisive: emit the
  corpus in both environments and hash the crates; identical hashes kill
  (c), then a per-crate warning dump names the one crate and kills (a)
  or (b).
  ACCEPTANCE: the one crate identified; the correct count established;
  whichever instrument is wrong fixed so CI and local agree; the
  baseline re-pinned only if 58 turns out to be the truth. Until then
  ratchets.yml is EXPECTED RED on the clippy step and FR-246's evidence
  says so explicitly.
