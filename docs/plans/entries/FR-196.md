- [x] FR-196 (opened and LANDED 2026-09-04; FR-193 item 7): **A STORE THROUGH
  A `std::vector` ELEMENT'S FIELD WAS SILENTLY DROPPED -- AND IT IS FR-189'S
  DEFECT, SAME BRANCH, SAME STAGE, ONE CONTAINER OVER.**
  `v[0].x = 99;` then reading it back gave native 99, emitted 1. The emitted
  Rust for the whole statement was `let _v6: P;` -- a dead, never-assigned
  binding. RHS side effects still fired, so the program looked alive and only
  the write was gone, which is the quietest possible failure.
  **THE HYPOTHESIS WAS CONFIRMED BY READING THE PRE-FIX IR, NOT INFERRED**,
  and the answer is more interesting than "same bug". The importer DOES emit
  the store:
      %7 = emitrust.subscript %0[%6] -> lvalue<struct<"P">>   the REAL place
      %8 = emitrust.load %7                                   copied OUT
      %9 = emitrust.variable ; assign %9 = %8                 into a temp
      %10 = emitrust.member %9["x"] ; assign %10 = 99         store onto the COPY
  It lands on a throwaway temp nothing reads, and the Rust emitter's
  dead-store/late-init pass then deletes BOTH the staging load and the store.
  So **"wrong value" (FR-189) versus "vanished statement" (here) is purely
  downstream -- it is whether DSE could prove the temp dead.** Same
  `emitMemberBasePlace` `f().m` branch, same staging, nothing new at a second
  stage. `v[i]` is a `CXXOperatorCallExpr` and `at`/`front`/`back` are
  `CXXMemberCallExpr`s, so all of them are `CallExpr`s and all were claimed
  by the branch FR-189 had to be inserted ahead of.
  FIX: one new branch in `emitMemberBasePlace`, placed immediately after
  FR-189's and AHEAD of the `f().m` branch, routing the base through
  `emitLValue`; `matchStlElementPlaceCall` recognises the STL element-place
  spellings **restricted to `isLValue()`** -- that guard is load-bearing, as
  a std method returning a prvalue must keep the materialize-into-a-temporary
  path. +85 lines, no deletions, structurally the analogue of FR-189's rather
  than a parallel mechanism that could drift out of agreement with it.
  Every spelling now byte-matches the clang++ native: `v[i].f`, `v.at(i).f`,
  `v.front().f`, `v.back().f`, nested `v[i].in.a` / `v[i].arr[k]` /
  `v[i].z++`, doubly-indexed `g[i][j].f`, the realistic loop (`total=0` ->
  `total=10`), and a cross-read `v[0].x = v[1].y + argc` with no E0499/E0502.
  The four adjacent shapes that already worked still do.
  **BONUS: `std::array<P,N>` `a[i].f` now WORKS**, where it was previously a
  hard rejection.
  ADDITIVITY PROVEN BY DIFFERENTIAL over ALL 1255 `.c`/`.cpp` files under
  `test/` plus `third_party/c-testsuite`, comparing IR bytes, stderr and
  return code: **changed = 1**, and that one is an IMPROVEMENT -- the
  pre-existing read `s += v[i].x + v[i].y` no longer stages two whole-element
  copies but projects straight off the subscript place (16 IR lines -> 10),
  and still byte-matches its native. Zero return-code changes, zero new
  rejections, no golden bytes moved.
  **A WORDING DEVIATION THE INCREMENT DELIBERATELY DID NOT "FIX", correctly.**
  `a.at(1).x = n` on a `std::array` now reports "member call receiver is not
  a recognized std::vector" where it used to say "std::array::at is not a
  recognized STL method". The new text names the wrong container and is
  worse -- but `test/RealWorld/run_realworld.py:511` BUCKETS on the substring
  "is not a recognized STL method", so changing it would silently reclassify
  RealWorld progress buckets. Left alone with the reasoning written into the
  invalid test's header so it is not rediscovered as a puzzle. That is the
  right call: a cosmetic improvement is not worth a silent measurement shift.
  Gate 986/986; clippy 101 -> 101 (+0); BOTH ratchets unmoved (CTestSuite
  220/220, Cpp17Suite 35/35 -- the plausible Cpp17 gain did not materialise,
  no case exercises the shape); epoch-6 re-verified intact; TRACTOR measured
  40/252, unchanged as expected for a C++-only path.
  NOT PROBED: `std::deque`/`std::list` element member places (rejected at
  type mapping long before this path); `std::vector<bool>`'s proxy
  `operator[]` (excluded by the `isLValue()` guard, and its elements have no
  members to project); and `m[k].f` on a `std::map` (the recognised value set
  contains no struct types, so the shape is unreachable today).

