- [x] FR-78 Differing-aggregate-arm unions: type-level containment with
  COMPLETE access interception (Track 5 cascade-containment Stage 3 —
  "30.7% of the cascade and the real netif blocker": lwIP's
  `union { ip6_addr_t; ip4_addr_t; }` and every C99-44 "union arm
  cannot alias the storage slot" rejection cascades its whole record).
  PRIOR ATTEMPT ON RECORD, REJECTED: the Track 5 union placeholder
  "silently emitted invalid Rust with no diagnostic in its first cut,
  and even guarded it breaks 42 crates with E0609 while buying 16
  items" — the guard did not intercept every access path. This FR is
  GO only if the spike proves the interception can be made COMPLETE:
  the union type imports as an opaque storage member (largest-arm
  sizing, or a marker type in the FR-52 style), records containing it
  import (the containment win — netif's record and its cascade), and
  EVERY access path through any arm — member read/write, nested
  projection, address-of, initializer, copy, aggregate init,
  argument/return positions, sizeof-dependent shapes — is enumerated
  and yields a LOCATED rejection at the access site; whole-record
  copies where the union travels as an opaque blob may be admitted
  only if the emitted Rust provably compiles (Copy + no field
  access). The emitter must refuse any leaked placeholder access
  (marker contract) so an unenumerated path fails loudly at
  emission, never as rustc E0609. NO-GO is a recordable outcome if
  the access-path enumeration cannot be closed — record the measured
  hole list. Spike must ALSO measure the containment YIELD on the
  live external corpus (cJSON/tinycrypt have few such unions — clone
  lwIP src/core for the netif/ip_addr shape as the motivating
  fixture) so the increment is justified by measured demand, not the
  historical table alone. Gates: Import pins (record containing a
  differing-arm union IMPORTS; every enumerated access shape rejects
  located with pinned wordings; whole-record copy admitted iff proven);
  emitter backstop pin (errors.mlir-style leaked-access refusal);
  EndToEnd byte-diff with a contained union the program never
  accesses (record passed around, other members used); external lwIP
  evidence (records import that previously cascaded); full lit 100%;
  the C99-44 one-slot matrix and its pins UNCHANGED (this FR only
  touches the shapes one-slot rejects).
  **SPIKE VERDICT: GO (2026-08-14).** The model: an ALL-AGGREGATE-arm
  union that would hit the :786 alias rejection imports as
  `struct_def @U ["opaque"] [array<Nxui8>] {emitrust.opaque_union}` —
  N = getTypeSizeInChars (C sizeof incl. tail padding, keeping the
  sizeof folds consistent), NO new ops; the >32-length explicit-
  Default machinery fires as predicted and whole-record copies /
  by-value args / returns / zero-init globals (actor and localized
  routes) all byte-diffed clean. The historical E0609 death mode was
  RECONSTRUCTED: leaked arm access passes the VERIFIER and translate
  silently — the emitter backstop is load-bearing, and the concrete
  importer leak was found by reading (emitRecordInitFields' union
  branch emits the active arm's MemberOp with no slot lookup — braced
  initializers leak exactly the historical way). All 16 MemberOp
  creation sites classified: 2 needed new checks, 1 belt, 4 C++-only
  (out of scope, C++ keeps rejecting), rest unreachable. Trigger
  scoped to all-arms-aggregate ONLY, which left every C99-44 matrix
  pin literally byte-unchanged (the AGG pin is aggregate+scalar).
  Active-arm initializers reject located this wave (zero-init
  admits); lwIP's nonzero ip_addr macro constants stay dropped —
  liftable later via serializeAPValueBytes. LWIP YIELD MEASURED
  (shallow clone, shipped headers, ZERO glue): only THREE distinct
  root unions existed corpus-wide (ip_addr, netif_ext_callback_args,
  ra_options — all all-aggregate shapes); 53 roots / 385 cascade-
  blocked items (37% of unported) flip category: the cascade wording
  VANISHED, 10 units gained ported items — and the removal UNMASKED
  the next measured frontier: extern-global constants of newly-
  importable types (IP_ADDR_ANY, 16 units, pre-existing unit-level
  behavior verified with a union-free control) and netif's own
  pointer members. Full suite 606/606.

