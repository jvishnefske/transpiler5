- [x] FR-87 Hosted byte-family calls accept member-array regions
  (the FR-86 census's bin D, generalized: `memset(s->buf, 0, n)` /
  `memcpy(s.a, s.b, n)` where the region argument is a MEMBER
  array — emitCharRegionArg resolves regions via emitPointerRValue
  only, with no member interception, so the shapes FR-74/86 admit
  as slice ARGUMENTS still reject as memset/memcpy REGIONS; the
  tinycrypt site ctr_prng.c:275 additionally needs the FR-72
  element rules for its u32 element — measure whether the u32
  member-array region is admissible byte-wise or stays rejected).
  The fix composes existing machinery: the char-region resolution
  gains the FR-74/86 member-array place matcher (dot/arrow, nested,
  decomposed roots, offset cursors), yielding the region slice at
  the member place + cursor; element agreement per FR-72 (i8/ui8
  helper selection; non-byte elements — measure and decide: a u32
  member array as a memset DESTINATION is byte-fillable exactly
  (memset semantics are bytes) IF a byte-view of the u32 array can
  be expressed safely — the FR-83 byte-view precedent may apply,
  else located rejection). Aliasing guard extends with the
  (VarDecl, field-path) key from FR-74. Gates: Import pins
  (memset/memcpy/memcmp over byte member arrays, dot/arrow/nested/
  offset; the u32-element outcome as measured) + frontier arms;
  EndToEnd byte-diff (member-array memset/memcpy, mutation visible,
  argc-seeded, vs clang native); external re-probe (ctr_prng.c:275
  measured); full lit 100%; C path only.
  **SPIKE VERDICT: GO (2026-08-15).** The member-region interception
  lands in emitCharRegionArg BEFORE emitPointerRValue, reusing the
  FR-74/86 matcher and the FR-72 helper images — zero new ops. The
  u32 question resolved as a GATED ADMISSION: memset over a u32
  member array admits only under const count % 4 == 0 + const fill
  byte, with the fill word computed as b * 0x01010101 — the
  byte-diff CAUGHT a wrong hand-picked replication constant
  (0xAAAAAAAA vs correct 0xABABABAB), and later caught a REAL
  MISCOMPILE mid-implementation (the same-field copy_within borrow
  taken at the dst cursor instead of 0, shifting the overlapping
  memmove window) — two saves by the supreme oracle in one wave.
  Same-(base,field-path) memcpy/memmove lowers to member-place
  copy_within (the naive dual borrow is rustc E0502); memcmp is
  exempt (shared borrows byte-diff clean) and pins the SIGN only.
  Byte-region-classified roots, impure cursors, mut-through-shared,
  u32 memcpy/memcmp and non-const/non-multiple counts stay located.
  ctr_prng.c:275 (u32 words) and :276 (byte V) both admitted.
  Full suite 629/629.

