- [x] FR-76 Fn-ptr components through the parameter mapper (Track 5
  cascade-containment Stage 2 — the recorded plan: "turns 47 of 80
  roots into working callbacks"; 58.8% of measured deduped root record
  rejections are a function-pointer field whose SIGNATURE names a
  pointer). Today a fn-ptr type's components map through the plain
  type mapper, so `void (*cb)(uint8_t *buf, unsigned len)` funnels
  into the mapType pointer residual and rejects the whole record.
  The change: fn-ptr COMPONENT signatures run through the same
  region-typed classification FR-75 gave body-less requirement
  signatures — an arithmetic-pointee scalar-pointer param component
  maps to `&mut [T]`/`&[T]` (constness from the pointee qualifier),
  yielding an importable FnPtrType over slice refs. THE UNIFICATION
  RISK the spike must resolve: a real C function ASSIGNED into the
  field maps per its own body-driven classification — if the body
  says ScalarRef where the fn-ptr type says slice, the signatures
  mismatch. Options the spike differentiates: (a) admission gated on
  assignment-site signature equality (mismatched assignment = located
  rejection — conservative, never wrong); (b) classification
  unification (a function whose address is taken into a slice-typed
  fn-ptr field forces its params slice-typed — the collectSliceParams
  call-argument precedent). CALL side: invoking the fn ptr passes
  region views exactly like an FR-75 requirement call. Frontier stays
  located: fn-ptr components naming struct pointers, void* without a
  consensus source, nested fn-ptrs, variadic signatures. Gates:
  Import pins test/Import/C/fnptr-slice-components.c (field decl,
  assignment of a compatible function, invocation through the field,
  const form) + fnptr-slice-components-invalid.c frontier
  (mismatched-classification assignment per the chosen option,
  struct-pointer component, variadic); EndToEnd byte-diff
  test/EndToEnd/fnptr-slice-components.c (callback table over byte
  buffers, argc-seeded, vs clang native); external re-probe evidence
  on cJSON's hooks (cJSON_Hooks has fn-ptr fields naming void* — may
  need the consensus rule; evidence, not gate); full lit 100%;
  C path only.
  **SPIKE VERDICT: GO (2026-08-14).** The design entry missed a DIALECT
  prerequisite the spike found: FnPtrType's verifier rejected slice-ref
  components ("invalid fn_ptr parameter type") — isValidComponentType
  now admits Ref/MutRef-of-Slice, with rendering and call_indirect
  needing ZERO changes (hand IR round-trips; the spliced callback-table
  crate byte-diffed the clang native in BOTH argc branches). The
  unification question resolved EMPIRICALLY for option (b)-scoped:
  loop-body callbacks already classify slice (exact match), but
  deref-only bodies classify ScalarRef, direct-called ones get Owner
  promotion, and global-array users get CellSlice — pure equality
  gating would reject the natural callback table, so address-taken-
  into-slice-typed-fn-ptr FORCES arithmetic-pointee ScalarRef->Slice
  and DISQUALIFIES Owner promotion (planFnPtrAliases hoisted before
  planOwners — it is pure-AST; CellSlice deliberately WINS over forcing
  and falls to the located assignment mismatch, since no &mut [u8] can
  come from a Cell-backed static). Call side grew a borrow loop in
  emitIndirectCall (emitCall precedent, aliasing guard included).
  Component classification is one level deep (a SaveAndRestore guard
  keeps nested-fn-ptr-with-pointer frontier) and !CPlusPlus-gated.
  Charter-sanctioned pin advance: fn-pointers-invalid.c's component
  case pinned exactly the shape this FR makes a feature — moved
  forward to a struct-pointer component, same wording. cJSON's hooks
  stay frontier on their void* RESULT type (not a param question).
  Full suite 593/593.
  EXTERNAL RE-PROBE (same day): ported 97 -> 100 on the tinycrypt/cJSON
  corpus — and the probe EXPOSED FR-77 (below): tinycrypt ecc.c became
  the corpus's first CRATE_NOBUILD (rustc E0425), the fail-loudly gap
  at the recovery seam that FR-76's type admission made reachable.
  POST-FR-77 CORRECTION: 17/17 crates build again and ported settles at
  97 — the "100" was inflated by the BROKEN crate's own progress JSON
  (it claimed 750-permille ported while unbuildable). An honest
  denominator beat a flattering one, again.

