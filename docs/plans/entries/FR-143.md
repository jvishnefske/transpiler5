- [x] FR-143 DEFECT (found while landing FR-141, 2026-08-28; FIXED
  2026-08-28): NO OWNER METHOD HAD EVER BEEN COUNTED AS PORTED.
  `collectEmittedSymbols` (tools/emitrust-cc/ProgressReport.cpp:487-493)
  iterated only `module.getBody()->getOperations()`, so a method living inside
  an `emitrust.impl` was never in the emitted set. A correctly ported owner
  method reported `"status": "missing"`, `"blocker": "unreached-by-import"`
  while its body sat in the crate.
  THE SCALE OF THE MISREPORT, measured on a real corpus project rather than a
  toy: `test/RealWorld/Inputs/union-find.c` reported 2 of 6 items ported
  (33%). It is in fact 100% ported -- `uf_node_init`, `uf_find`, `uf_union`
  and `connected` are all in the crate, inside `impl OwnerMainNodes`. Post-fix
  it reads 6/6, `ported_permille` 1000. The ledger was understating a fully
  ported program by two thirds.
  BLAST RADIUS, measured old-binary vs new-binary over 24 projects (the whole
  RealWorld C corpus, the whole RealWorld C++ corpus, and 5 Driver shapes):
  FOUR changed, ALL UPWARD, all `missing` -> `ported` (union-find 2/6->6/6,
  logger 1/3->2/3, and two Driver shapes). NOTHING went down; no count
  decreased anywhere. All 6 RealWorld C++ projects are unchanged, because C++
  member functions are deliberately not item-graph nodes (ItemGraph.h:83-87).
  NO RATCHET MOVED, and this was the risk worth checking rather than assuming:
    - c-testsuite and Cpp17Suite record transpile/build/run PASS-FAIL lists and
      never read the progress artifact. Re-ran both: 220/220 and 35/35,
      unchanged.
    - The RealWorld PER-ITEM ledgers were the one place a count change could
      have forced an update -- `run_realworld.py:1104` treats "newly ported and
      not in the ledger" as a FAILURE demanding `--update`. It did not fire,
      for two independent reasons: `score_items` is set only for
      `--corpus-kind=cpp`, and none of the 6 C++ projects changed.
    - `search_never_worse.py` compares `--search` against plain on the SAME
      binary and runs on the C++ corpus only, where every count is identical.
      Note the FR-43 search probe uses this same collector, so the search
      objective function now sees owner methods too.
  TRAIT IMPLS ARE DELIBERATELY EXCLUDED, and the reason is the interesting
  part: `FuncToEmitRust.cpp:190-192` RENAMES an imported C++ destructor to the
  bare `drop` to satisfy the `Drop` trait. That is the one in-impl name that is
  not the item symbol, so admitting trait impls could credit a graph node
  spelled `drop` with a destructor's evidence -- the SAME artifact untruth this
  FR removes, in the OVER-reporting direction. Nothing is lost: destructors are
  not graph nodes, and every item that is one lands in an INHERENT impl.
  Verified the exclusion changes no measured count.
  A REJECTION ROW STILL OUTRANKS IN-IMPL EVIDENCE, verified by adversarial
  probe: a free `Counter_bump` colliding with `Counter::bump` is REJECTED and
  reads `dropped`, never `ported`, because `buildProgressReport` consults the
  rejection map before the emitted set. That ordering is what makes descending
  into impls safe.
  FR-141's negative control (test/Driver/incremental-owner-struct-reached.c)
  was flipped from `missing` to `ported` DELIBERATELY -- its comment had
  recorded that it pinned the wrong-but-pre-existing reading and that the fix
  belonged here. The comment now says FR-143 changed it, and the file gained a
  totals pin, so a widened FR-141 sweep would still have to break it.
  Pinned additionally by test/Driver/incremental-owner-method-ported.c (the
  direct inverse: a reached owner method reports `ported` and is counted) and
  incremental-owner-method-cpp.cpp (the C++ side, which is a NEGATIVE control
  plus the adversarial name-clash probe -- C++ member functions are not graph
  items, so there is nothing to flip there and the honest test is the one that
  pins why the descent is safe).
  CONSEQUENCE FOR THE LEDGER, worth stating plainly: every ported count
  recorded in this file before 2026-08-28 UNDERSTATES the owner-promotion path.
  Any cross-wave comparison of ported counts spanning a change in owner
  promotion is suspect and should be re-measured rather than trusted.

