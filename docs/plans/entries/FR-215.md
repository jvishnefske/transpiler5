- [x] FR-215 (opened and SPIKED 2026-09-09; FR-174's top-ranked sub-item):
  **`pointer type outside a parameter position` IS NOT A MECHANISM, THE
  AMPLIFICATION THAT EARNED IT THE #1 RANK IS NOT WHERE FR-174 THOUGHT, AND
  THE LARGEST AMPLIFYING PIECE OF IT IS NOT A POINTER ITEM AT ALL. NO-GO AS
  RANKED.**
  RE-DERIVED AT HEAD (3f1ce0f), same method as FR-174 -- 501 pinned systemd
  objects, per-unit `--incremental` ledgers unioned, deduped by
  `(abs file:line:col, symbol)`, cascades attributed to their `blame_chain`
  root: **1,592 -> 1,103** (direct 353 -> 291, gated 1,239 -> 812). **The item
  is 31% SMALLER than filed**; the pointer family's SHARE held (66% -> 69.2%).
  Absolute counts are not comparable across the two dates -- the denominator
  is 9,842 here against FR-174's 13,342 and that gap is unresolved -- so the
  ratios are the comparable figure.
  **THE RANKING PREMISE IS NOW FALSE.** FR-174 ranked this first as "the ONLY
  one with real amplification at 3.5x". At HEAD, by amplification (TOTAL>=30):
  `union arm cannot alias the storage slot` **81.0x** (243/3),
  `volatile-qualified type` **13.0x** (117/9), `FILE* only supported as a
  function-local` **11.3x** (34/3), and only then this item at 3.8x.
  **IT IS THREE MECHANISMS, AND THE AGGREGATE 3.79x IS A BLEND OF 1.03x, 8.1x
  AND 38.6x** -- established by instrumenting the single terminal residual
  (`ImportCTypes.cpp:383-385`) with a spike-only breadcrumb and re-running the
  identical 501-unit census:
      607  61 direct  9.95x  fn-ptr component `void *` -- callback userdata
      205 199 direct  1.03x  ARRAY ELEMENT is a pointer (178/199 `const char *const`)
      193   5 direct 38.60x  `top` -- pointer arm of an ANONYMOUS UNION
       82  21 direct  3.90x  fn-ptr component, incomplete record pointee
  **THE 38.6x BUCKET IS A UNION ITEM WEARING POINTER WORDING.** All three
  amplifying sites are pointer arms of an ANONYMOUS union
  (`NLAPolicy.policy_set` gates 133, `sd_json_variant.parent` 48,
  `NLAPolicySetUnionElement.string` 7): `collectRecordFields` flattens the
  anonymous union and calls `mapType` on the arm directly, BYPASSING
  `mapStructFieldType`'s data-pointer -> i64 shortcut, and FR-167 phase 1's
  trial/rollback then replays the flatten-path diagnostic verbatim. A 17-line
  reproducer hits it at plain HEAD. Rolled into the union family this is
  **508 items behind ~50 direct sites** -- the real successor to FR-166/167,
  and it is FR-167 phase 2, already written up.
  **THE COSTED INCREMENT IS SAFE AND NET-NEGATIVE, which is why this is a
  NO-GO rather than a small win.** Classifying a fn-ptr `void *` component as
  the same inert i64 cursor a data-pointer struct member gets is 15 lines, one
  file, one level deep, and keeps FR-171's tripwire honest (verified in the
  gate, not assumed). Measured over identical 501-unit regenerations: **299
  items resolved, 303 newly surfaced -- ledger NET +4, the WRONG DIRECTION.**
  60 symbols went blocked -> clean and **0 went clean -> blocked**; EndToEnd
  byte-diff 325/325 green with zero golden churn; the whole-program 501-object
  link is rc=0 with zero unresolved externals; `cargo build --release
  --offline` on the 12.6 MB crate is clean. The only 2 lit failures are the
  negative tests pinning the removed rejection. It is a good change that buys
  nothing.
  **TRACTOR IS +0 STRUCTURALLY, NOT MERELY MEASURED**: `pointer type outside a
  parameter position` appears **ZERO times** in the 202 TRACTOR `EMIT_FAIL`
  reasons, so no increment anywhere in this sub-item can move that number.
  **AND TRACTOR'S REAL FRONT IS SOMETHING ELSE ENTIRELY: `aliasing mutable
  pointer arguments`, 80 of 252 cases (32%).** Nothing in FR-174's five
  sub-items competes with it.
  FR-61f-c's "REPRESENTATION, not aliasing" **HALF transfers, checked rather
  than assumed** (the error FR-178 made about a different finding). The gate
  asks no aliasing question -- `:383` is a bare `isPointerType()` -- so
  FR-136's points-to NO-GO is NOT re-litigated by any of this. But
  `fnptr-param/void*` is not a representation problem either: the
  representation already exists TWICE (`mapStructFieldType` and
  `ParamKind::Carrier` both give i64), it was an unwritten classification arm.
  Whereas `array-elem` IS representation, and the evidence is the real tools
  refusing hand-written IR at parse time: `error: invalid array element type
  '!emitrust.ref<!emitrust.slice<i8>>'` from BOTH `emitrust-opt` and
  `emitrust-translate` (`EmitRustTypes.cpp:79-83`), plus the value model
  giving each pointer ONE base where a table needs N. So that one is a
  DIALECT change, not a type-level one.
  FR-168's over-count does NOT apply at the direct level: **0 of the 291
  direct sites are `ported` in any other TU**, i.e. TU-invariant. Raw-to-
  distinct fan-out is **7.7x** (2,229 raw -> 291), higher than FR-165's 5.6x
  aggregate, so anyone reading raw counts here is ~8x wrong.
  RECOMMENDED INSTEAD: (1) **FR-167 phase 2, the anonymous-union pointer
  arm** -- 508 items behind ~50 sites at 81x and 38.6x; (2) a
  `const char *const` string table lowered to the already-supported padded
  `[[i8; W]; N]` form -- ~165 items, no new op or dialect change, ceiling
  measured by hand-rewriting the C, but it must carry two named miscompile
  fences (a NULL hole must stay a LOCATED rejection at 3 sites, since padding
  NULL to `""` flips `!!table[i]`; and bare `sizeof(table)` changes from `N*8`
  to `N*W` and must be rejected).
  **A CORRECTION TO MY OWN HARNESS RECIPE, found chasing this entry's
  "missing header (openssl/conf.h) at 48" note.** The documented
  `--extra-cflags=-I<openssl-dev>/include` invocation **breaks the cmake
  configure step**: with it, all 202 EMIT_FAIL cases report a single
  `cmake configure failed` reason instead of their real blockers, while PASS
  stays 41/252. And the header is worth +0 anyway -- exactly ONE corpus file
  includes openssl (a SPHINCS+ `rng.c`), so the 48 are SPHINCS+ variants where
  it is merely the FIRST failure, and FR-178 measured that bundle at +0 twice
  because each case carries 3-5 further blockers. **A sixth instance of the
  first-failure trap.**
  NOT RESOLVED: the 13,342 -> 9,842 denominator gap; the true pure-table count
  (between 164 and 181, the scanner runs pre-preprocessor); whether the
  82-item incomplete-record-pointee bucket collapses into FR-174's third
  sub-item; the clippy delta of the `void*` prototype.

