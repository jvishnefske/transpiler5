- [x] W2.13 by-value-capture lambda via LAMBDA LIFTING (task-010; flips
  Cpp17Suite 00903; VALIDATED 2026-08-14: full lit 560/560, ledger exactly
  22 PASS / 4 UNSUPPORTED / 0 MISCOMPILE). Implementation notes beyond the
  spike sheet, all recorded in code comments: lifted bodies QUEUE and drain
  after the enclosing importFunction completes (the shared body emitter's
  per-function state is single-occupancy — the emitVaClone secondary-
  prologue precedent); the two-capture import golden identifies cells by
  their stored constants, not alloca position (entry allocas hoist in
  reverse declaration order); the escape-by-copy frontier (`auto g = f;`)
  pins the use-scan while escape-by-template-arg keeps the pre-existing
  top-level rejection; symbol spelling follows fnRustName over the
  enclosing function's Rust name (c_main_add_base), matching what
  cFunctionSymbolName actually produces. `auto f = [a, b](int x) { ... };` lifts to a
  module-level synthesized fn with the captures PREPENDED as parameters
  (`<function>_<name>` mangle, the block-scope convention), the captures
  FROZEN at the declaration point (`let vN = a;` — one load per capture at
  creation, which IS C++'s capture-by-value semantics; verified against the
  native even when the source variable mutates between creation and call),
  and every `f(args)` call rewritten to `lifted(frozen..., args...)`. The
  Rust-closure route is a measured NO-GO: the dialect has no closure value —
  `emitrust.literal` is a fixed string with no operand splicing, and final
  local spellings are emitter-owned (uniquified), so a literal naming a
  capture textually could silently bind a different variable — the exact
  forbidden silent-wrong-code shape. Recognizer gate: explicit by-value
  captures of SCALAR locals, non-mutable, non-generic, and every use of the
  lambda local is a direct `operator()` call; anything else keeps a located
  rejection (all seven adversarial shapes probed rejecting today: mutable,
  `[&]`, `[=]`, escape-by-arg, escape-by-return, zero-capture-to-fnptr —
  a separate pre-existing gap — and zero-capture direct call, which falls
  out of the lift for free). The closure record must be intercepted BEFORE
  VarDecl type conversion or it leaks into aggregate import (today's
  rejection source: the operator() member-shape gate,
  ImportCAggregates.cpp:449). Body import binds the operator() body's
  DeclRefExprs — which point at the ENCLOSING VarDecls, not closure fields
  (verified AST fact) — to the prepended params via `symbols`; a small
  `importLiftedLambda` builds the FuncOp (importFunction cannot be reused
  verbatim: the prepended params exist in no FunctionDecl).
  **SPIKE VERDICT: GO (2026-08-11).** Differential probe: the hand-lifted
  C++ equivalent already imports to EXACTLY the target IR shape (the lift's
  target is the pipeline's native dialect). Hand-written intended IR
  (existing ops only) round-trips emitrust-opt print->parse and renders
  through emitrust-translate to Rust that is BYTE-IDENTICAL to the clang++
  native for both 00903 ("34 42\n") and the mutation-adversarial variant
  (freeze-let carries the creation-point value; the dead post-freeze store
  elides legally). No new op, no new attr.
  Gates: corpus 00903 UNSUPPORTED->PASS, ledger exactly 22 PASS /
  4 UNSUPPORTED / 0 MISCOMPILE; new pins test/Import/Cpp/stl-lambda.cpp
  (lifted fn shape, freeze-lets, call rewrites, two-capture ordering) and
  stl-invalid.cpp frontier additions (mutable, reference capture, default
  capture, escape); new test/EndToEnd/stl-lambda.cpp byte-diffs the built
  crate against clang++ including a capture mutated between creation and
  calls with an argc-derived seed; full lit 100%; C path untouched.

