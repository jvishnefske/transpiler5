- [x] FR-91 The widened-corpus decay census (tiny-AES-c +
  heatshrink joined the external corpus 2026-08-15: 3 units, all
  emitting crates at 37/69 aggregate ported, and ArrayToPointerDecay
  x9 tops their residual with shapes the FR-74..90 machinery
  APPEARS to cover — root-causing required): (1) aes.c's
  `KeyExpansion(ctx->RoundKey, key)` / `memcpy(ctx->Iv, iv, n)` —
  member arrays through an arrow root to defined callees and hosted
  regions, exactly FR-74/87's shapes, yet rejecting: find which
  gate declines (root classification? the FAM-bearing struct? the
  cast `(state_t*)buffer`?); (2) heatshrink's
  `memmove(&hse->buffer[0], &hse->buffer[k], n)` — same-field
  memmove with RUNTIME offsets (FR-87's copy_within image with
  runtime ranges — admissible if the range expressions are pure);
  (3) `memset(hsd->buffers, ...)` where buffers is a FLEXIBLE
  ARRAY MEMBER — FAM regions are a recorded type-level concession
  (the field omission precedent); measure whether a FAM region can
  be admitted at all this wave or stays located. Spike: census all
  9 sites, root-cause (1), land the provably-byte-safe subset,
  record the rest. Gates: Import pins per landed shape + frontier
  arms (measured wordings); EndToEnd byte-diff; external re-probe
  (the 3 new units' fractions measured); full lit 100%; C path
  only.
  **SPIKE VERDICT: GO (2026-08-15).** Root cause (1) confirmed as
  the CTS-BR boundary: an all-u8 struct classifies as a byte-region
  record and matchMemberChainRoot's byte-region guard declined the
  member matcher (differential probe: adding one `int tag` to the
  same struct made it import). Bins (2)+(3) COLLAPSED: the
  fixed-extent twins of every heatshrink shape — including
  runtime-offset memcpy and same-field runtime memmove — import
  today; the FAM leaf (no static extent) is the sole blocker for
  all 5 heatshrink sites, recorded NO-GO this wave
  (allocation-backed trailing representation = its own future FR).
  Landed: member-array windows on BYTE-REGION roots ride the
  byte-region machinery — `slice_of [mut] region[cursor+constOff]`
  open-ended windows (over-wide is faithful: every byte-family op
  carries explicit n and slice callees never read a length);
  same-root dst+src pairs ride __emitrust_memcpy_within_u8 with
  absolute cursors (byte-diffed both overlap directions);
  aliasing keys on the ROOT with an empty field path (two windows
  of one region with a mut collide — rustc E0502 measured — and
  stay located). BYTEREC frontier arm flipped positive; the
  two-window BYTEREGION arm stays rejected. External: aes.c
  13->16/24 as projected (next frontier: Cipher's state_t* body);
  heatshrink unchanged (FAM); tinycrypt all still building.
  Full suite 642/642.

