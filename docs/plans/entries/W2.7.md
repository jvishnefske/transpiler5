- [x] W2.7 `std::array<T, N>` (task-004). Spike verdict GO on a BETTER
  route than the planned opaque-`[T; N]` family: `mapStdLibraryType` maps
  the specialization to the SAME `!emitrust.array<NxT>` a C `T[N]` maps
  to, so every existing array path — per-element aggregate init, subscript
  places in read and write position, struct fields, multi-dim nesting, the
  emitter's whole `[T; N]` rendering — applies unchanged; NO
  isStlOpaqueType widening, NO index-place generalization, NO
  emitDefaultValue branch (the plan's task-004 checklist items for the
  opaque route are all obsolete on this route). Importer-side deltas are
  exactly three: (1) the type map case (template args <Type, Integral>,
  zero-length rejected); (2) the aggregate initializer's one-level
  struct-wrapper peel in emitLocalVar (`std::array`'s semantic
  InitListExpr is struct-shaped — one member, the inner `T[N]` list —
  gated on the new `isStdArrayRecordType` so no C list is ever peeled);
  (3) `size()` in emitStlMemberCall folding to the compile-time constant N
  (index-typed, cast to the call's declared C type via emitLenCall's
  convention), plus operator[] on an `!emitrust.array` receiver in
  emitLValue building the identical `emitrust.subscript` place a C `a[i]`
  builds (the C++ size_t index conversion rides along as an
  `emitrust.cast` to ui64). Every other std::array method keeps a located
  `std::array::<name> is not a recognized STL method` rejection. R1
  re-probe after the type became mappable: 00603's structured binding
  still rejects located ("copy/move construction" on the holding value),
  no silent path opened.
  Gates: corpus 00401-00402 flipped UNSUPPORTED->PASS, Cpp17Suite ledger
  ratcheted to exactly 11 PASS / 15 UNSUPPORTED / 0 MISCOMPILE; new pins
  test/Import/Cpp/stl-array.cpp (mapped type, wrapper peel, subscript
  write place, constant-N size) and test/EndToEnd/stl-array.cpp
  (byte-diff vs clang++: subscript writes that always change the stored
  value, runtime index from a parameter, two seeds, double array with
  %.2f); the 43-test C++ slice passes; C path untouched (the peel is
  std::array-gated, the map case fires only inside isInStdNamespace
  dispatch).
  (test/Import/Cpp/stl-array.cpp; test/EndToEnd/stl-array.cpp;
  test/Cpp17Suite/expected-pass.txt)

