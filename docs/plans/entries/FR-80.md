- [x] FR-80 Address-carrying const requirements (the measured lwIP
  unlock: all 7 const-global src/core failures take `&ip_addr_any` —
  dhcp.c alone 14 times — via the IP4_ADDR_ANY macro family; a
  getter-BY-VALUE cannot express pointer identity). Candidate image
  for the spike to de-risk or refute: the trait item returns a
  static borrow — `fn ip_addr_any() -> &'static IpAddr` — giving the
  consumer-supplied constant a STABLE ADDRESS, so `&g` lowers to
  `E::g()` (a place-producing call), argument positions taking a
  const-struct-pointer param receive it directly, and pointer
  EQUALITY (`p == &ip_addr_any`, the ip_addr_isany idiom) compares
  identity faithfully because &'static of one consumer item is
  unique. THE RISK SURFACE the spike must enumerate before any GO:
  (1) the pointer plan models addresses as region/cursor facts —
  where does `&<requirement-global>` fit; today it rejects with
  use-shape wordings (returned-address, taking-the-address,
  pointer-comparison) BEFORE finalize, so the qualifying scan runs
  in pointer-plan territory, not the FR-70 loop; (2) STORING the
  address into a struct field hits the i64-cursor field model — that
  shape likely stays rejected (measure how much lwIP demand needs
  it); (3) mixed usage (same global both value-read and
  address-taken) needs one coherent image (a &'static getter serves
  both: read = `*E::g()`); (4) trait-item mutability/lifetime
  spelling must stay dyn-free and unsafe-free per the FR-52
  contract; (5) the FR-42/FR-58 defer and link paths must not
  regress. NO-GO with the measured blocker list is a fully
  acceptable outcome — this is the deepest pointer-plan integration
  attempted. Gates (if GO): Import pins (address-taken const-struct
  extern qualifies with the &'static item; the three pointer-plan
  wordings flip only for qualifying shapes; store-into-field and
  non-const stay verbatim); trait golden grows the &'static
  spelling; EndToEnd lib-crate byte-diff exercising argument
  passing, deref-read, and pointer equality against a clang native
  given the same definitions; lwIP re-probe evidence (the 7 units
  un-error — measured, not assumed); full lit 100%; C path only.
  **SPIKE VERDICT: GO (2026-08-15).** Both feared NO-GO pivots
  resolved cheap: the POST-lowering surface needs ZERO new ops
  (call_opaque ref value + deref + member + load + addr_of covers
  arg-pass, deref-read, and the interior-member IP4_ADDR_ANY chain),
  and the lifetime fix is RENDERING-ONLY — trait-item ref results
  render `&'static` in emitTraitDef while RefType stays bare `&`
  everywhere else (the sole illegal byte was the E0106 trait line;
  bodies are legal as-emitted). One small dialect addition WAS
  required pre-lowering: `emitrust.global_addr` (no op could yield a
  global's address — GlobalLoadOp pins result==value type), verifier
  const-target-only, with the marker-contract emission refusal.
  The lwIP recount corrected the demand table: 35 address-take
  lines, dominant shape the interior-member chain, ZERO
  pointer-identity compares and ZERO address-into-field stores in
  the corpus — so compare and store-into-field STAY located
  (pinned), and one return-position use (dns.c) stays rejected but
  the unit recovers under --incremental. Shared-borrow admission is
  confined to BODY-LESS (requirement) callee params — extending
  `&T` to defined functions' const-struct params would shift bytes
  across every golden (the param model is written against `&mut`);
  measured cost: etharp stays rejected. EndToEnd identity oracle:
  the consumer implements the check via core::ptr::eq against a
  same-VALUE different-IDENTITY decoy. MEASURED UNLOCK: lwIP
  src/core 20 -> 24 units importing (dns, netif, tcp, igmp
  un-errored); udp/dhcp now surface the previously-masked NON-const
  `IP_DATA` blocker (x10 units — the next demand item), etharp the
  scope cost above. Full suite 611/611.

