- [x] FR-98 FAM-tail locals through Option-member projections (the
  FR-97-named front: `int16_t *const index = hsi->index;` at
  heatshrink_encoder.c:426, where hsi is itself the FR-96
  member-read binding of the Option<HsIndex> field — an FR-93/95
  window-backed pointer local whose ROOT is reached through the
  FR-96 per-use projection). The composition: the local's backing
  becomes the projected member's tail Vec (each USE projects
  `hse.search_index.as_mut().unwrap().index[cursor]` fresh — the
  FR-96 per-use discipline extended through one more member link
  plus the tail subscript), with cursor arithmetic exactly as
  FR-93/95. The borrow surface repeats FR-96's finding one level
  deeper: uses of the tail local interleaved with other hse
  borrows in do_indexing's loop must stay per-use-fresh (spike
  measures the real loop against rustc). Frontier: escaping,
  address-of, cross-root reassignment as before. Gates: Import
  pins (the binding, indexed read/write through it in a loop,
  interleaved with other member uses) + frontier arms; EndToEnd
  byte-diff (the do_indexing-shaped chain walk, argc-seeded, vs
  clang native); external re-probe (do_indexing measured —
  encoder 23/28 the target, honestly restated if another wall
  appears); full lit 100%; C path only.
  **SPIKE VERDICT: GO (2026-08-15) — the "another wall" clause
  FIRED and both walls landed.** The lift simulation showed the
  entry's single-front framing under-scoped: eliding the index
  local alone left do_indexing blocked on a SECOND wall — the
  `data = hse->buffer` local whose ROOT is a slice-region PARAM
  (the base place is lvalue<slice<struct>>, which the member
  projector didn't handle) — and 23/28 needed BOTH arms. Arm A:
  famMemberLocal bases resolve via a fresh per-use
  emitFamOptionMemberProjection then the existing FAM-tail Vec
  projection (no new rendering). Arm B: slice-region-param roots
  subscript by the root's OWN cursor cell first, with the
  MANDATORY root-motion gate — a root whose region hasArithmetic
  rejects located, because a walked root (hse++ between binding
  and use) would silently follow the moved cursor, a miscompile;
  the F5 probe is the regression pin and must never import.
  Direct chains were already legal and NLL-safe by
  field-disjointness one index-projection deep; escape/addr-of/
  mid-loop-rebind measured post-impl as CORRECT compositions
  (pinned positive); cross-root join keeps its located wording.
  Byte-diff: the interleaved loop (tail Vec write + buffer window
  read + last[] read/write + call) identical at both argc seeds.
  EXTERNAL: encoder 22 -> 23/28, do_indexing PORTED; the 5
  residual encoder walls are exactly the predicted set (alloc's
  returned pointer, poll's non-address member, pointer-param-
  outside-deref pair, st_f). Full suite 665/665.

