- [x] W2.8 `std::pair<T1, T2>` (task-005; R2 spike REQUIRED and run).
  Spike verdict: route (b) GO — the specialization imports as an
  importer-SYNTHESIZED real struct through the ordinary importRecord
  machinery, reusing every existing struct path (member places read/write,
  by-value returns, parameter passing, derives) with ZERO emitter changes;
  route (a) (opaque Rust tuple + a new tuple-member emission path) was not
  needed and was never prototyped past the initial survey once (b)
  byte-diffed clean on both corpus entries at first build. Mechanics:
  (1) `mapStdLibraryType`'s pair case maps both element types, derives a
  shape-keyed struct name from their Rust spellings (`Pair_i32_i32`,
  `Pair_i32_f64`; rejected if a spelling is not identifier-safe), and
  PRE-SEEDS `assignedStructNames` before calling importRecord — distinct
  instantiations all spell `pair` in clang, so an unseeded import would
  collide. (2) The record's fields are genuinely just `first`/`second`,
  but its METHODS (operator=, defaulted copy ctor, converting ctors) would
  trip the W2.2 member-shape gate and the importCXXMethods copy-ctor
  rejection — both now SKIP std-namespace records wholesale (std methods
  are intercepted at call sites, never imported; the base-class data-loss
  check stays unconditional). (3) The two-argument value constructor
  imports field-wise (`emitPairConstructInit`, mirroring
  emitDefaultConstructInit's member-place shape), reached from both the
  declaration position (emitCXXConstructInit, checked before the
  imported-ctor lookup) and the VALUE position (emitRValue's
  CXXConstructExpr case: anonymous temp place + field init + whole load —
  C++17's guaranteed elision means the native leg also performs exactly
  one construction, so the temp models identical observable behavior;
  probed the MaterializeTemporaryExpr concern from the plan: the
  returned-pair AST carries the construct directly, no separate handling
  needed). Copy construction (`auto [q, r] = p`'s holding value, 00601)
  still rejects located — the R1 guarantee holds after the type became
  mappable. Emitted Rust is idiomatic: `struct PairI32I32 { first, second
  }` with the standard derives, `divmod` returning it by value.
  Gates: corpus 00501-00502 flipped UNSUPPORTED->PASS, Cpp17Suite ledger
  ratcheted to exactly 13 PASS / 13 UNSUPPORTED / 0 MISCOMPILE; new pins
  test/Import/Cpp/stl-pair.cpp (struct_def synthesis, field-wise ctor,
  member write, temp-and-load value construction, two coexisting
  instantiations) and test/EndToEnd/stl-pair.cpp (byte-diff vs clang++:
  always-changing writes, two factory seeds, mixed pair<int,double>);
  the 45-test C++ slice passes; C path untouched (all new paths are
  std-record-gated).
  (test/Import/Cpp/stl-pair.cpp; test/EndToEnd/stl-pair.cpp;
  test/Cpp17Suite/expected-pass.txt)

