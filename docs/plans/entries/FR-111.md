- [x] FR-111 QUALITY: the `field_reassign_with_default` fuse is
  OVER-gated for Drop types. TranslateToRust.cpp:2440 bails on
  `bindingHasDrop`, because a `..S::default()` functional-update base
  would construct and drop a spurious `S` (W2.17, measured). But the
  fuse already computes `coversAllFields` at :2504-2510 and drops the
  base entirely in that case, so the guard can narrow to "has Drop
  AND not all-fields". Worth 2 warnings; removes a hazard class.
  LANDED 2026-08-23 (spike GO). One correction to this entry: the
  coversAllFields computation lives in emitFusedFieldInit, a
  DIFFERENT function than the guard, so it was not reusable in
  place -- the landed shape recomputes coverage at the analysis site
  (post-prefix-loop in computeFieldInitFuses) via the module-level
  SymbolTable lookup (NOT lookupNearestSymbolFrom: emitrust.impl is
  its own symbol table and impl-method bodies would mis-resolve),
  refusing the fuse unless the struct_def resolves AND the fused
  prefix covers every field; emission agrees automatically off the
  same vector. Polarity fixed by construction: a droppy binding
  never reaches the "unresolved keeps the base" fallback (unsafe
  direction for Drop). The W2.17 hazard re-measured live (guard
  removed: spurious `dtor 0` before the first program line) and its
  partial-init fence is now PINNED for the first time, alongside the
  all-fields fused shape (base-free, lost mut, byte-identical
  including drop order across a branch scope) and the prefix-broken
  shape. Clippy delta exactly as filed: -2 (both warnings lived in
  cpp-destructor-deadstore.cpp, whose emitted shape changed and
  whose byte-diff stayed green); partial-droppy warnings remain BY
  DESIGN -- correctness over lint. Gates: full meson suite 807/807
  (fast 569 + EndToEnd 238).
  (test/Target/Rust/field-reassign-default.mlir droppy pins;
  test/EndToEnd/cpp-dtor-field-init-fuse.cpp; the
  cpp-destructor-deadstore.cpp intent note)

