- [x] FR-94 Flexible-array-member records with allocation-backed
  tails (heatshrink's whole residue — the FR-91 NO-GO's recorded
  direction: `struct heatshrink_decoder { ...fields...; uint8_t
  buffers[]; }` allocated as `malloc(sizeof(S) + buf_sz)`, with
  every buffers[] access sized by runtime fields). Census-first:
  (a) how FAM records import today (the FAM field omission
  concession — what survives, what rejects where); (b) the
  allocation-site shape (heatshrink_decoder_alloc's
  malloc(sizeof + n) — the FR-64/65 heap-recognition precedent:
  can the allocation be recognized and the FAM tail represented
  as an owned growable/fixed byte tail — Vec<u8> or boxed slice —
  carried alongside the struct fields, with buffers[] accesses
  lowering to tail indexing?); (c) the ownership plumbing (who
  frees; the free-site recognition; pass-by-pointer of the FAM
  record through the existing owner/struct conventions with the
  tail riding along). This may need a representation decision on
  the order of FR-78's blob (a synthesized struct with a tail
  field) — the spike differentiates and NO-GO is fully
  recordable. Gates (if GO): Import pins per admitted shape +
  frontier arms; EndToEnd byte-diff (alloc/use/free lifecycle,
  argc-seeded, vs clang native); external re-probe (heatshrink's
  5 FAM sites + unit fractions measured); full lit 100%; C path
  only.
  **SPIKE VERDICT: GO (2026-08-15).** Representation (i) won: the
  FAM record's struct_def grows a trailing opaque Vec<u8> field —
  the tail rides every existing convention automatically; the
  vec_fill init FOLDS into the struct-literal (buffers: vec![0u8;
  n], ..default()), zero new ops. Measured constraints that
  shipped: the derive line dropped Copy for non-Copy-opaque
  fields (unconditional Copy was rustc E0204 on otherwise-correct
  code); tail extent = allocSize - offsetof(FAM) with a located
  gap-layout gate (a padding-gap FAM is constructible where C
  legally indexes past the syntactic n; heatshrink is gap-free);
  WHOLE-STRUCT ASSIGNMENT of tail-carrying records became a
  located rejection (C's FAM assignment is fields-only — .clone()
  would diverge — the one previously-admitted shape this FR
  regresses, pinned); the free wrapper types owned-by-value
  (use-after-free = rustc E0382, the safe direction); FAM-record
  GLOBALS reject located (the actor owner lift would wrap the
  non-Copy record in a Copy owner — a new frontier wall the spec
  missed). Honest externals: decoder 8->12/17 (alloc itself does
  NOT lift — its early parameter-validation return NULL is a real
  behavioral path an owned return cannot represent; only
  elidable-guard NULLs admit); poll/search_index residues named.
  BONUS: the admitted malloc(sizeof+CONST) over-count shape was a
  LATENT MISCOMPILE (constant-true null branch, measured) — now
  routed to the byte-diff-proven owned representation.
  Full suite 654/654. Measured post-land: encoder 20/28,
  decoder 12/17.

